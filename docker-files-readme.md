# This directory contains docker files

 - golang-clean
    Build clean bash / zsh environment with go (for testing compromise / etc)

```
$ ./golang-clean/build-all.sh [-f FORCE REBUILD]
$ ./golang-clean/start-bash.sh
or
$ ./golang-clean/start-zsh.sh
```
